language: cpp
compiler:
  - gcc
  - clang

before_install:
  - sudo add-apt-repository -y ppa:mapnik/boost
  - sudo add-apt-repository -y ppa:irie/boost
  - sudo apt-get update
  - sudo apt-get install  -qq cmake  libboost-all-dev libssl-dev

before_script:
  cmake .

script:
  make

branches:
  only:
    - master
notifications:
  recipients:
    - microcaicai@gmail.com
  email:
    on_success: change
    on_failure: always

