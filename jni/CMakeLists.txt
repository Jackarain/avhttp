cmake_policy(SET CMP0078 NEW)
cmake_policy(SET CMP0086 OLD)
#include(UseSWIG)
include(${SWIG_USE_FILE})
set(SWIG_MODULE_NAME "simplehttpjni")
set(CMAKE_SWIG_FLAGS -package com.example.sh)
include_directories(${CMAKE_CURRENT_SOURCE_DIR})
set_source_files_properties(simplehttp.i PROPERTIES CPLUSPLUS ON)
swig_add_library(${SWIG_MODULE_NAME} LANGUAGE java SOURCES simplehttp.i)
swig_link_libraries(${SWIG_MODULE_NAME} simplehttp-static log)
